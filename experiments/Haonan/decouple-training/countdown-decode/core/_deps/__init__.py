"""Vendored, self-contained dependencies for the countdown-decode experiment.

GENERATED by vendoring; do not hand-edit the algorithmic content. Two sources:

* ``decode_core`` — the domain-agnostic trellis decoder (savi/greedy/best_of_k/
  oracle) + the ``Domain`` interface + its own vendored ``_deps/metrics``. Copied
  verbatim from ``verifier-decode/decode_core``; it imports NO domain code.
* ``countdown`` / ``sampler`` / ``instances`` — the M1 Countdown domain (Phi =
  (sorted values, target), the executor, the exact reachability oracle), the GPU-free
  mock emission backend + op-text parser/renderer, and the curated/seeded instance
  generator. Copied from ``verifier-decode/m1-countdown-trellis/core``; the only edit
  is the intra-package import path (``from core.countdown`` -> ``from _deps.countdown``)
  so the tree imports cleanly with this package's parent (``countdown-decode/core``)
  on ``sys.path``.

The ``countdown_domain`` module here is NEW (not vendored): a ``CountdownDomain``
adapter satisfying ``decode_core.domain.Domain`` by delegating to the vendored
``countdown`` + ``sampler`` (mirrors the test-only adapter in
``verifier-decode/decode_core/tests/_countdown_adapter.py``).

The public surface is re-exported by the parent package ``core`` (see
``countdown-decode/core/__init__.py``); sibling modules import from there.
"""
